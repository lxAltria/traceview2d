
# include_directories("include")

# find_package (NetCDF REQUIRED)
# include_directories (${NETCDF_INCLUDE_DIR})

# find_package (GLEW REQUIRED)
# include_directories (${GLEW_INCLUDE_DIR})

# find_package (OpenGL REQUIRED)

# find_package (Qt4 REQUIRED COMPONENTS QtCore QtGui QtOpenGL)
# include (${QT_USE_FILE})


# add_executable(main2 main2.cpp widget.cpp trackball.cpp)
# set_target_properties (main2 PROPERTIES AUTOMOC ON)
# target_include_directories(main2 PRIVATE ${PROJECT_SOURCE_DIR}/include)
# target_link_libraries(main2 ${QT_LIBRARIES} ${OPENGL_LIBRARIES} ${GLEW_LIBRARY} ${NETCDF_LIBRARIES})
